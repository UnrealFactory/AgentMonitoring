---
id: WORK-0071
title: The toggle's escape hatches are one record wide — the session choice belongs to the toggle alone
agent: d3-human-view-builder
status: done
started: 2026-08-22T18:05:00Z
finished: 2026-08-22T20:18:19Z
tags: [dual-record, ui, d3]
refs: []
files: [src/lib/recordView.ts, src/components/HumanView.tsx, src/pages/WorkDetailPage.tsx, src/pages/BugDetailPage.tsx, src/pages/NoteDetailPage.tsx, src/pages/AppFeedbackPage.tsx, src/lib/human.ts, src/lib/i18n/en.ts, src/lib/i18n/ko.ts, src/styles/app.css, src/styles/tokens.css, scripts/check-keys.mjs]
---

## What

Round 3 of D3 (the Agent / Human toggle on every record screen). The gap the round-2 critic
failed the piece on: the human-empty box's own escape button, and the correction line over a
retelling, both called `setRecordView` — the session-wide setter the toggle uses — from
controls whose own labels are scoped to one record.

## Why

SPEC names one behaviour for this toggle: "the choice persists for the session". A reader who
pinned the human half and walked into a record written before human areas existed was shown
the designed box, pressed its only button, and lost the pin for the rest of the window: the
next retold record opened on the agent area, and the one after that, with nothing on screen
saying the pin had been thrown away. src/lib/recordView.ts's own module doc states the
opposite rule in as many words.

## How

`useRecordView` gained an ephemeral per-record override — `peekAgent()` / `peeking`, held as
the record's id in component state, so it expires by arithmetic when the address changes and
never touches `sessionStorage`. The empty box's button and the correction line call it
instead of `choose("agent")`; `choose` remains wired to the toggle alone (grep: no other
caller in src/). While the override is up the agent half carries one quiet line —
`.view-peek`, `role="status"`, `view.peekNote` — naming the record and saying that which half
the next record opens on is unchanged. Pressing Human takes it down; the keyboard lands on
the Agent segment when the button it pressed disappears.

Three more from the same review:

- the board's toggle spoke record-language (`view.label` = "Which half of this record to
  read" over a surface that draws no record; `view.humanNoneTip` on an all-bare board, an inch
  above a notice that got the same fact right). `retold` is now what tells a board from a
  record — the only surface that can count retold rows is a board — and picks
  `view.boardLabel` / `view.boardAgentTip` / `view.boardHumanTip` / `view.boardHumanNoneTip`.
- a note's `description` is drawn on both halves again (round 1 and 2's unfixed finding). It
  is a frontmatter field and the lead SPEC screen 8 names, not part of the body; on the human
  half `.note-lead.is-human` takes the sheet's measure so the two left edges line up.
- `.human-sheet .figure` steps to `--weight-semibold`: `.figure` marks with weight *and* ink,
  and the sheet's prose is already `--text-primary`, so half the mark was a no-op there.

`carveTakeaway` now joins a soft-wrapped final paragraph before measuring it — a closing line
lost its accent block purely because the author's editor wrapped at 80 columns.

Gates: `npx tsc --noEmit` 0, `npm run build` 0, `check-keys` 271/271 live and 279/279 against
a fixture that still has legacy records (the four new assertions live there), `check-errors`
0, `check-clipping` clean ko+en on live (117 screens x 8 widths) and on the fixture (20 x 8),
`check-i18n` clean en / 39 findings ko — all 39 the pre-existing kebab-id-inside-a-Korean-
sentence class, 38 of them on screens that predate this piece.

## Outcome

**Shipped**: the two record-scoped escape hatches — the human-empty box's button and the
correction line over a retelling — no longer touch `agentmon.recordView`. `useRecordView`
returns `peekAgent()` / `peeking`, a component-local override keyed to the record's id;
`choose` (the session setter) is wired to the toggle and to nothing else in src/. While the
override is up the agent half carries `.view-peek` (`role="status"`), naming the record and
saying that the next record's opening half is unchanged; the keyboard lands on the Agent
segment when the button that vanished had it.

Also: the app-feedback board's toggle speaks board-language (`view.boardLabel`,
`view.boardAgentTip`, `view.boardHumanTip`, `view.boardHumanNoneTip`, chosen by the presence
of a retold count — only a board has one); a note's `description` is drawn on both halves
(`.note-lead.is-human` at the sheet's measure); `.human-sheet .figure` steps to
`--weight-semibold`; `carveTakeaway` joins a soft-wrapped closing paragraph before measuring
it.

**Verified**: `npx tsc --noEmit` and `npm run build` clean. `check-keys` 271/271 against the
live project in ko and en, and 279/279 against a CLI-built fixture that still carries records
with no human area — where the four new assertions run: the box's button leaves the stored
choice at "human", the screen says so naming the record, the next retold record still opens
on the human half, and the correction line leaves the choice alone too. A 35-check browser
probe in both languages covered the same walk plus the board labels, the chips and
`[[note-name]]` links inside a retelling, and the lead's alignment to the sheet.
`check-clipping` clean ko+en on the live project (117 screens x 8 widths) and on the fixture
(20 x 8, which is where the empty box and the board notice live); a bespoke probe measured
`.view-peek` at the same eight widths in both languages — no clipping, no line breaking a
Korean word. `check-errors` clean. `check-i18n` clean in en.

**Known gaps**: `check-i18n --locale ko` is red at 39 findings, unchanged in kind from the
last two rounds — every one is the same class, an English kebab id (`start-here`,
`note-name`) inside a Korean sentence, in `.rel-group-hint` on ten note pages and in
`a.ref-inline` chips inside WORK-0061/0062's own prose. One names a screen this piece added
(`[human view start-here @1600]`) and is the same element and string as the finding above it,
because the 관련 항목 rail is drawn on both halves. It wants its own ticket: the fix is either
the sweep learning that a record id is not a word, or those strings not embedding one.
`ArrowRight` still does not move the toggle's selection — an app-wide gap shared by all six
tablists in src/, not this piece's. The sheet leaves ~240px of empty column at 1600px, which
is the reading measure doing its job.
