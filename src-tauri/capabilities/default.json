{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main AgentMonitoring window: read the vault through our own commands, open a folder picker to relocate the vault, and open referenced files in the system handler.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
