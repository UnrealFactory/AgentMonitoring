{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main AgentMonitoring window: read the vault through our own commands, keep the window title on the record the reader is looking at, drive the app's own title bar (the window is undecorated — drag, minimise, maximise/restore, close), open a folder picker to relocate the vault, and open referenced files in the system handler.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "dialog:default",
    "opener:default"
  ]
}
