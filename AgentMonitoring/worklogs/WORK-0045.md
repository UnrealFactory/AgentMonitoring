---
id: WORK-0045
title: "App feedback: the board where agents report the tool itself"
agent: feedback-board-builder
status: done
started: 2026-08-21T06:38:00Z
finished: 2026-08-21T07:05:52Z
tags: []
refs: []
files: [crates/agentmon-core/src/feedback.rs, crates/agentmon-core/src/model.rs, crates/agentmon-core/src/lib.rs, crates/agentmon-cli/src/main.rs, mcp/lib/tools.mjs, mcp/test/server.test.mjs, docs/MCP.md, src-tauri/src/lib.rs, scripts/project-fs.mjs, scripts/vite-project-api.mjs, src/lib/api.ts, src/lib/types.ts, src/pages/AppFeedbackPage.tsx, src/App.tsx, src/components/Sidebar.tsx, src/lib/i18n/ko.ts, src/lib/i18n/en.ts, src/styles/app.css, scripts/check-i18n.mjs, crates/agentmon-core/templates/claude-md.ko.md, crates/agentmon-core/templates/claude-md.en.md, SPEC.md, docs/AGENT_MANUAL.md]
---

## What

A fourth record kind, about the app rather than any project. Core:
crates/agentmon-core/src/feedback.rs — FB-NNNN.md files in ~/.AgentMonitoring/feedback
(beside the registry, same AGENTMON_REGISTRY_DIR override), frontmatter id/title/type
(bug|idea)/agent/status (open|done)/created/done + free prose body, ids under the
folder's lock, atomic rewrites, open-first newest-first ordering. CLI: agentmon
app-feedback add/list/view/done/reopen — machine-level, works from any directory, no
--dir. MCP: a seventh tool, app_feedback (type, title, optional body — and no dir, the
one tool without it); tools/list grew 6,070 → 6,664 bytes against a budget raised
consciously to 7,168, docs/MCP.md renumbered. App: list_app_feedback /
set_app_feedback_status Tauri commands, /project-api/app-feedback[/:id/status] in the
browser middleware (answered before any project resolution, so it works with zero
projects served), an App feedback board at /app-feedback (type filter in the URL, done
items dimmed at the bottom, Mark done / Reopen), and a sidebar entry under 로컬 with the
open count. Both CLAUDE.md templates now point agents at app_feedback for complaints
about the system itself. SPEC.md (storage + CLI surface) and AGENT_MANUAL.md (command
reference) updated; check-i18n walks the new screen, exempts feedback titles/bodies as
author content, and knows MCP / app_feedback as technical tokens.

## Why

The owner wanted one place to read what agents wish this app did and where it failed
them, then work through it — and filing had to be mistake-proof about scope, which named
the thing: app_feedback, not feedback, because a tool named feedback would collect
project feedback (owner's own catch). Machine-level rather than in any project because
the subject is this install of the app; storing it in a repo would make app feedback
travel with whichever codebase happened to host it.

## How

The store is the project store's rules at small scale — create_new ids under the lock,
temp-and-rename writes, done as a state change instead of a delete (what was asked for
stays evidence). agent/human split by convention and affordance: the tool and CLI add
file items; done/reopen live on the board and in two human-shaped CLI verbs. The first
test run leaked two items into the real ~/.AgentMonitoring: unit tests sandboxed via the
process-global AGENTMON_REGISTRY_DIR env while the registry's own corrupt-file test
mutated the same variable from a parallel thread. Fixed by refactoring the module to
explicit-directory functions (add/list/view/set _in) with the env read confined to
one-line wrappers — tests now touch no global state; the leaked files were deleted.

## Outcome

Verified on every layer. cargo test 113 pass, including the feedback lifecycle
(sequence ids, empty-body wish, open-first ordering, done/reopen with refused no-ops,
clean not-found on a board that does not exist) — rewritten to explicit-dir functions
after the env-race leak, and the every-command-documents-an-example CLI gate satisfied.
CLI smoke: add bug + idea, list, done, filtered --json list, view — all in a sandboxed
registry. MCP: 114 pass; tools/list 6,664 of 7,168 bytes; the new section proves items
land beside the scratch registry and never inside the project, and an unknown type
surfaces the CLI's own refusal. Browser transport driven by Playwright: two seeded items
render with kind pills, the sidebar shows 앱 피드백 with open count, Mark done dims and
sinks the item, the type filter lives in the URL (?type=bug); screenshot inspected.
check:i18n clean in both locales (158 screens, the new board walked; feedback
titles/bodies exempt as author content); check:clipping clean at all widths (80
screens). tsc clean; release CLI rebuilt. The leaked FB files were removed from the real
~/.AgentMonitoring/feedback, and the leftover pre-change dev server on 5173 (whose stale
code answered the board's route with a 400) was killed. Not run: check:keys — the board
adds no context menu and no palette surface, and the sidebar link is a plain NavLink.

