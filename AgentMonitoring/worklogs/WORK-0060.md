---
id: WORK-0060
title: "v1.0.3 ships: essential notes reach installed apps"
agent: fable-updater-splash
status: done
started: 2026-08-21T13:32:35Z
finished: 2026-08-21T13:32:35Z
tags: [release]
refs: [WORK-0059]
files: []
---

## What

Bumped 1.0.2→1.0.3 across the three version files, committed (b97c4ed), pushed, and published GitHub release v1.0.3 with npm run release — AgentMonitoring_1.0.3_x64-setup.exe, tag and asset from the same commit.

## Why

The essential note type (WORK-0059) changes the app's notes screen and the session-start contract; installed apps only meet it through a release.

## How

The handoff note's recipe, unchanged from v1.0.2. handoff-v1-release rewritten to v1.0.3 facts, including that updates from ≤1.0.1 still show the console updater while 1.0.2+ uses the splash.

## Outcome

v1.0.3 live: gh release view shows tag v1.0.3 @ main with AgentMonitoring_1.0.3_x64-setup.exe; release.mjs exit 0. Installed apps offer it on their next check.

## For humans

Version `v1.0.3` of the app is out, and installed copies are offered it the next time they check.

What rides in it is a new kind of note called `essential` — required reading. It changes what the app's notes screen shows, and it changes what an agent is told to do when a session opens. None of that reaches an installed copy any other way: a published release is the only route.

I raised the number from 1.0.2 to 1.0.3 in the three files that have to agree on it, saved that, and ran the one command that builds the Windows installer and publishes it. It finished without an error. GitHub's own record of the release shows `v1.0.3` on the main line of work with `AgentMonitoring_1.0.3_x64-setup.exe` attached, both built from the same code.

The steps were the same as last time, so I rewrote `handoff-v1-release`, the shared note that tells the next agent how to cut a release, to describe this version. That includes which updates still show the old black command window: any coming from 1.0.1 or earlier. From 1.0.2 onward people see the small installing panel instead.

Nothing an agent writes reaches an installed app until somebody publishes it.

